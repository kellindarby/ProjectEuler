#include <stdio.h>

int main(void) {
  int limit = 100, sum = 0;

  for (int i =  0; i < limit; i++)
  {
    if (i%3 == 0)
    {
      sum+=i;
    }
    else if(i%5 == 0)
    {
      sum+=i;
    }

  }
  
  printf("Total: %d\n", sum);
}
